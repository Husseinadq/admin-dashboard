# Vue.js Project .gitignore

# ===================================================================
# Dependencies & Lock Files
# ===================================================================
# The node_modules directory contains all of your project's dependencies.
# It can be huge and should always be regenerated by running `npm install` or `yarn install`.
/node_modules

# Package manager cache and lock files (optional, some teams commit lock files)
/package-lock.json
/yarn.lock
/pnpm-lock.yaml

# ===================================================================
# Build & Distribution Files
# ===================================================================
# The output of the build process (e.g., from `npm run build`).
# This is generated code and should not be version controlled.
/dist
/build
/.output
/public/build

# ===================================================================
# Environment Variables
# ===================================================================
# NEVER commit your secret keys, API tokens, or passwords.
# Keep them in local .env files.
.env
.env.local
.env.*.local

# It's a good practice to commit a template file so other developers
# know what variables are needed. Use ! to un-ignore a file.
!.env.example

# ===================================================================
# Logs & Debugging
# ===================================================================
# Log files from npm, yarn, or other tools.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ===================================================================
# IDE & Editor Directories
# =---===============================================================
# Configuration files for popular code editors.
.vscode/*
!.vscode/extensions.json
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# ===================================================================
# Testing & Coverage
# ===================================================================
# Output from test runners like Jest, Vitest, Cypress, etc.
/coverage
/cypress/videos/
/cypress/screenshots/

# ===================================================================
# Operating System Files
# ===================================================================
# System files created by macOS, Windows, and Linux.
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# ===================================================================
# Temporary Files & Caches
# ===================================================================
# Caches from tools like Vite or Nuxt.
.vite
.nuxt
.cache
*.temp
*.tmp
*.swp